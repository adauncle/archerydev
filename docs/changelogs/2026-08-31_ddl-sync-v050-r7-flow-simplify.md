# 8/31 DDL 跨库同步 v0.5.0-r7 精修 (当前流程图简化 7 步→3 步) (8/31 15:11)

## 背景

- 8/31 15:05 v0.5.0-r6 DML 实时性改 T+1 (commit `ead4b11`) 落地
- 8/31 15:11 DBA 阿达叔叔拍板 1 处精修: **当前流程简化下: 业务 RD → DBA执行 → 凭经验判断 → DBA手工到历史库跑 SQL** (3 步走, 省略 Archery / 业务库 / 通知等中间环节)

## 决策

**R 之前 DBA 视角当前流程图, 从 7 步简化成 3 步**:
- 原 7 步: 业务 RD → ①提 DDL 工单 → Archery → ②审批 → 业务库 → ③执行 ALTER → Archery → ④通知 DBA → DBA → ⑤凭经验判断 → ⑥手工跑 SQL → 历史库 (+ ⑦ 科大国创 DML)
- 新 3 步: 业务 RD → ①提 DDL 工单 → DBA → ②凭经验判断 → ③手工到历史库跑 SQL → 历史库

**关键简化**: 删 Archery / 业务库 / 通知 DBA 等"自动化环节" (R 之前流程里这些环节虽然是 Archery 平台提供的, 但痛点不在这些, 痛点在"DBA 凭经验 + 手工" 这 2 步), 重点突出"DBA 凭经验 + 手工" 是当前最大风险.

## 改动

**3 处统一改 "7 步 → 3 步"**:

| 文件 | 行 | 原 | 改 |
|------|----|----|----|
| `2026-08-28_ddl-sync-pair-design-v050-r1.md` | 24-48 | 24 行 ASCII art 流程图 (6 列布局: 业务 RD → Archery → 业务库 → Archery → DBA → 历史库 + 科大国创 DML) | 16 行 ASCII art 流程图 (3 列布局: 业务 RD → DBA → 历史库), 突出"DBA 凭经验 + 手工" |
| `2026-08-28_ddl-sync-pair-design-v050-r1.md` | 54 | `R 之前 DDL` 详细对比表链路 "业务 RD → Archery → 业务库 DDL → DBA 收通知 → DBA 凭经验 → DBA 手工到历史库跑 SQL" | 简化成 "业务 RD 提 DDL 工单 → DBA 凭经验 判断 → DBA 手工到历史库跑 SQL" (跟新流程图对齐) |
| `2026-08-28_ddl-sync-pair-feature-card.html` | 440-442 | 1 行流程图 "业务 RD ──①提 DDL 工单──▶ Archery ──②审批 (3 级) ──▶ 业务库 ──③执行 ALTER──▶ Archery ──④通知 DBA──▶ DBA ──⑤凭经验判断──▶ DBA ──⑥手工到历史库跑 SQL──▶ 历史库 + ⑦ 科大国创 DML" | 简化成 "业务 RD ──①提 DDL 工单──▶ DBA ──②凭经验判断──▶ DBA ──③手工到历史库跑 SQL──▶ 历史库" |

**保留不动**:
- MD 优缺点表 (✅ 1 优点 + ❌ 5 缺点): 流程特性分析, 跟流程图互补
- HTML 优缺点表 (✅ 1 优点 + ❌ 5 缺点): 同 MD
- R 之后 v0.5.0 优化后流程图: 保留 10 步详细版 (Archery 自动 + 钉钉通知 + rollback 是 v0.5.0 新增的, 重点是讲清楚"自动化了什么")

## 跟 r6 关系 (r7 精修叠加)

r7 是 r6 后的"流程图简化" (1 处图改), 不动 r6 的 T+1 改时间。r7 跟 r6 一起构成 v0.5.0-r7 完整版。

## 教训 (跨项目可复用, 1 条新增, 合并到 r1 entry 避免 memory 膨胀)

1. **设计稿"对比" 场景, 优化前流程要简化突出痛点, 优化后流程要详细讲清楚改了什么** — 8/31 r5 加 2 个流程图, 优化前 (R 之前) 写了 7 步详细流程, 优化后 (R 之后) 写了 10 步详细流程. 8/31 r7 用户拍板"当前流程简化下", 才发现 R 之前的 7 步详细流程把"Archery / 业务库 / 通知 DBA" 这些"自动化环节" 都列出来了, 跟"痛点是 DBA 凭经验" 不直接相关, 反而冲淡了痛点. **下次写二次开发设计稿"对比" 场景, 优化前流程要简化到"关键痛点步骤" (3-4 步), 优化后流程要详细到"新增了什么自动化环节" (5-10 步)**, 2 个流程图步数差越大, 优化价值越明显.

## 同源 entry

- 8/31 15:05 r6 DML 实时性改 T+1 (commit `ead4b11`) - 改的是 r5 加的流程图
- 8/31 14:45 r5 设计稿精修 (commit `6ee465c`) - 改了 2 个流程图, r7 是再精修 R 之前流程图
- 8/28 18:20 r4 背景重写 (commit `05c57cb`) - 加 §0 4 部分

## 下次推 prod 必做 (新增 1 条, 合并到 r1 entry)

1. **写二次开发设计稿"对比" 场景, 优化前流程简化到痛点步骤 (3-4 步), 优化后流程详细到新增环节 (5-10 步)** — 2 个流程图步数差越大, 优化价值越明显. 不要"详细版" 对"详细版" 写, 优化前要删跟痛点无关的步骤.
