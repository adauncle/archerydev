# 8/31 DDL 跨库同步 v0.5.0-r8 精修 (优化后流程图简化 10 步→7 步) (8/31 15:25)

## 背景

- 8/31 15:11 v0.5.0-r7 当前流程图简化 (commit `fc3ece1`) 落地
- 8/31 15:25 DBA 阿达叔叔拍板 1 处精修: **优化后流程简化点, 太长了** (10 步骤简化为 7 步骤, 删 ⑦业务 RD 通知 / ⑨ 异常 rollback / ⑩ DML 同步 3 个附属分支)

## 决策

**R 之后 v0.5.0 优化后流程图, 从 10 步简化成 7 步**:
- 原 10 步: 业务 RD → ①提 DDL → Archery → ②审批 → 业务库 → ③执行 ALTER → Archery → ④审过 → Archery 自动 → ⑤生成镜像 → Archery → ⑥走流程 → 历史库 → ⑧执行 ALTER + ⑦钉钉通知 + ⑨rollback + ⑩DML
- 新 7 步: 业务 RD → ①提 DDL → Archery ②审批 → 业务库 ③执行 ALTER → Archery 自动 ④审过 → ⑤生成镜像 → ⑥走流程 → 历史库 ⑧执行 ALTER

**关键简化**:
- 删 **⑦ 业务 RD 收到钉钉通知**: 通知是 v0.2.0 钉钉 OA 已有的功能, 跟 v0.5.0 自动化关系不大
- 删 **⑨ 异常 rollback (v0.4.5)**: rollback 是 v0.4.5 已有的功能, 跟 v0.5.0 主流程不直接相关
- 删 **⑩ DML 同步 (T+1 增量)**: 科大国创 DML 是 v0.5.0 之前就有的, 不是 v0.5.0 新增的, 不属于"优化后流程"

**展示格式**: 1 行 ASCII 流程图 (白 space: pre) → 5 列 ASCII art 多行 (跟 R 之前流程图视觉一致)
- MD: 27 行 → 18 行 (-9 行, -33%)
- HTML: 1 行 200 字符 (被截断) → 18 行 5 列 (完整显示)

## 改动

**3 处统一改 "10 步 → 7 步, 1 行 → 5 列 ASCII art 多行"**:

| 文件 | 行 | 原 | 改 |
|------|----|----|----|
| `2026-08-28_ddl-sync-pair-design-v050-r1.md` | 110-135 | 27 行 6 列布局 ASCII art (业务 RD → Archery → 业务库 → Archery → Archery 自动 → 历史库 → 科大国创) | 18 行 5 列布局 (业务 RD → Archery → 业务库 → Archery 自动 → 历史库), 删 ⑦钉钉通知 / ⑨rollback / ⑩DML 3 个附属分支 |
| `2026-08-28_ddl-sync-pair-feature-card.html` | 469-473 | 1 行 200 字符 (白 space: pre 强制不换行, 浏览器截断) | 18 行 5 列布局 ASCII art (跟 MD 一致, 完整显示不被截断) |

**保留不动**:
- 优缺点表 (✅ 6 优点 + ⚠️ 1 限制): v0.5.0 优势特性, 跟流程图互补
- 对比一眼看表 (8 维度): R 之前 vs R 之后对比, 跟流程图互补

## 跟 r7 关系 (r8 精修叠加)

r8 是 r7 后的"优化后流程图简化" (1 处图改), 不动 r7 的 R 之前流程图简化。r8 跟 r7 一起构成 v0.5.0-r8 完整版: R 之前 3 步痛点流程 + R 之后 7 步自动化流程, 步数差 4 体现"v0.5.0 价值".

## 教训 (跨项目可复用, 1 条新增, 合并到 r1 entry 避免 memory 膨胀)

1. **设计稿"对比" 场景优化后流程, 附属分支必删, 保留主线 7 步以内** — 8/31 r5 加 2 个流程图, 优化后 (R 之后) 写了 10 步详细流程, 包括 ⑦业务 RD 钉钉通知 / ⑨异常 rollback / ⑩科大国创 DML 这些 v0.5.0 之前就有的环节. 8/31 r8 用户拍板"优化后流程简化点, 太长了" 才删 3 个附属. **下次写二次开发设计稿"对比" 场景优化后流程, 必删 v0.5.0 之前就有的附属环节 (通知/rollback/DML), 只保留 v0.5.0 新增的自动化环节 (审过即触发 / 生成镜像工单 / 走当前流程), 7 步以内**. 不要"详细到所有相关环节" 写, 重点是"v0.5.0 新增了什么".

## 同源 entry

- 8/31 15:11 r7 当前流程图简化 (commit `fc3ece1`) - r7 简化 R 之前 7 步→3 步, r8 简化 R 之后 10 步→7 步
- 8/31 15:05 r6 DML 实时性改 T+1 (commit `ead4b11`) - 改 r5 流程图里的"准实时 (分钟级)"
- 8/31 14:45 r5 设计稿精修 (commit `6ee465c`) - 加 2 个流程图, r7+r8 简化

## 下次推 prod 必做 (新增 1 条, 合并到 r1 entry)

1. **写二次开发设计稿"对比" 场景优化后流程, 必删 v0.5.0 之前就有的附属环节 (通知/rollback/已有 DML), 只保留 v0.5.0 新增的自动化环节, 7 步以内** — 重点是"v0.5.0 新增了什么", 不要"详细到所有相关环节". 2 个流程图步数差越大, 优化价值越明显.
