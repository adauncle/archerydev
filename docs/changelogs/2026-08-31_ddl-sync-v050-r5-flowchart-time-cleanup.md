# 8/31 DDL 跨库同步 v0.5.0-r5 设计稿精修 (加 2 个流程图 + 全文去时间 + 删作者 + 删汇报口径)

## 背景

- 8/28 18:25 v0.5.0-r4 背景重写 (commit `05c57cb`) 落地, 跟领导汇报口径
- 8/31 14:45 用户拍板 3 处精修:
  1. **加 2 个流程图** (当前 DBA 人工 DDL 流程 vs 优化后 v0.5.0 自动 DDL 流程), 优缺点在图上方列出来, 对比清晰
  2. **整个文档中不要体现时间**, 只用 R1 R2 R3 R4 替代; **作者这一行删除**
  3. **去掉"给领导汇报口径"几个字** (领导看到不舒服)

## 决策

**v0.5.0-r1 设计稿 + HTML 功能图说, 3 处精修**:
- **加 2 个流程图** (R 之前 DBA 视角 + R 之后 v0.5.0 自动化视角), 优缺点在图上方列表展示
- **全文时间清理**: 8/28/8/21/8/27/2026-08-28 等所有日期 → R 之前/R1/R2/R3/R4 替代
- **作者删除**: MD `**作者**: mavis` + HTML hero-meta `作者 mavis` + HTML Footer `设计人 mavis` 全部删
- **汇报口径删除**: 去掉"给领导汇报口径" 几个字 (留"4 部分讲清楚必要性" 即可)

## 改动

### 1. MD 设计稿精修 (commit 准备)

**文件**: `docs/designs/2026-08-28_ddl-sync-pair-design-v050-r1.md`

#### 1.1 头部 (line 1-15) 改

- 删作者行: `**作者**: mavis` → 删除
- 删日期/原版日期/版本 3 行 → 合并 1 行 `**版本**: v0.5.0-r4 详细设计 (R1 → R2 → R3 → R4 完整版)`
- 标题: `v0.5.0-r1 修订设计` → `v0.5.0 详细设计` (跟版本对应)
- 配套链接 4 个 → 6 个 (加 R2 + R3 + R4 changelog)

#### 1.2 §0 头部 (line 17) 改

- `## 0. 背景 (给领导汇报口径 · R4 新加)` → `## 0. 背景 (R4 新加)` (删"给领导汇报口径")
- 引用 `> **R4 重写背景**: ...` → 简化, 删"跟领导汇报时不便讲清楚必要性"

#### 1.3 §0.1 (line 21) 改 + 加流程图 1

- 加 **当前流程图 (R 之前 · DBA 视角)**: 业务 RD → Archery → 业务库 → Archery → DBA → DBA → 历史库 (7 步 ASCII 流程图)
- 加 **当前流程优缺点表** (图上方): ✅ 优点 (DML 自动化) + ❌ 缺点 1-5 (DBA 漏同步 / 没校验 / 同步延迟 4-8h / 跨多库配错 / DML 同步挂)
- 加 **详细对比表** (R 之前 vs R 之后 v0.5.0): 4 流程对比, 一眼看

#### 1.4 §0.5 (新加) 加流程图 2

- 加 **优化后流程图 (R 之后 v0.5.0 · 自动化视角)**: 业务 RD → Archery → 业务库 → Archery → Archery 自动 → Archery → 历史库 (10 步 ASCII 流程图)
- 加 **优化后流程优缺点表** (图上方): ✅ 优点 1-6 (T+0 实时 / 0 漏同步 / 0 延迟 / 1-click 配表 / DML 永久稳定 / 工单跟正常一样) + ⚠️ 限制 (Phase 1 范围)
- 加 **对比一眼看 (R 之前 vs R 之后)**: 8 维度对比表 (DDL 同步方式 / 延迟 / 漏同步风险 / 业务方感知 / DML 同步稳定性 / DBA 工作量 / DBA 配表 / 异常保护)

#### 1.5 全文时间清理 (脚本批量)

**脚本**: `scripts/_archive/_r5_clean_time.py` (311 条 replace 规则, 替换 100 处)

清理的 5 类时间:
1. **8/28 + 数字:数字** (修订时间): 全部 → "R 拍板时"
2. **8/28 + 中文** (修订描述): 全部 → R1/R2/R3/R4
3. **8/21** (R 之前原版): 全部 → "R 之前"
4. **8/27** (实战案例): 实战时间 → "实战时", 实战描述 → "实战"
5. **2026-08-28** (ISO 日期): 全部 → "R 修订" (但文件路径保留文件名)
6. **小写 r1/r2/r3** (版本标识): 全部 → R1/R2/R3

### 2. HTML 功能图说精修 (commit 一起)

**文件**: `docs/designs/2026-08-28_ddl-sync-pair-feature-card.html`

#### 2.1 title (line 6) 改

- `<title>DDL 跨库同步 v0.5.0-r3 · 业务库 ↔ 历史库 · 功能图说 (R 拍板时 修订)</title>` → `<title>DDL 跨库同步 v0.5.0-r4 · 业务库 ↔ 历史库 · 功能图说</title>`

#### 2.2 Hero 段 (line 376-413) 改

- hero-tag: `v0.5.0-r3` → `v0.5.0-r4`
- hero-tag: `R 拍板时 修订` → `R4 完整版`
- hero-tag: `r2 一键配 6 min` → `R2 一键配 6 min`
- hero-tag: `r3 走当前配置流程` → `R3 走当前配置流程`
- h1: `v0.5.0-r3` → `v0.5.0-r4`
- hero-sub 4 行: 全部 r1/r2/r3 → R1/R2/R3
- hero-meta: **删 `作者 mavis` 整段**
- hero-meta: `R 修订 修订` → `R 之前 → R1 → R2 → R3 → R4 完整版`
- hero-meta: `v0.5.0-r1` → 改为 `v0.5.0 · Archery 二次开发设计稿`
- hero-meta: `v0.5.0 原版 → R2 修订` → `R4 背景重写`

#### 2.3 §00 (line 415-466) 改 + 加 2 流程图

- 注释 `<!-- ===== 00 背景 (给领导汇报 · 4 部分) ===== -->` → 删"给领导汇报"
- section-sub `给领导汇报口径: <b>当前两个流程...</b>` → 删"给领导汇报口径: "
- **加流程图 1 (R 之前 DBA 视角)**: ASCII 流程图 + 优缺点表 (图上方)
- **加流程图 2 (R 之后 v0.5.0 自动化视角)**: ASCII 流程图 + 优缺点表 (图上方)
- ④ 痛点 5 段: `r3 拍板` → `R3 拍板`, `r2` → `R2`, `r3` → `R3`

#### 2.4 其他位置 (line 620+ 批量)

- line 620 `r1 批量导入 ... r2 1-click` → `R1 批量导入 ... R2 1-click`
- line 628 `批量导入 — 8/28 核心新增` → `R1 核心新增`
- line 808 `R 拍板时 r2 核心` → `R2 核心`
- line 921 `r3 决策` → `R3 决策`
- line 931 `r2 核心洞察` / `r1 批量导入` → `R2 核心洞察` / `R1 批量导入`
- line 941 `r2 核心` / `r1 fallback` → `R2 核心` / `R1 fallback`
- line 1034 `#12345 业务库工单 8/26 19:30` → `#12345 业务库工单 [实战时]`
- line 1108 `r2 一键配` / `r1 批量导入` → `R2 一键配` / `R1 批量导入`
- line 1170 `R 拍板时` → 删
- line 1186 `r1 vs r3 拍板对比` → `R1 vs R3 拍板对比`
- line 1191-1192 `r1 拍板 (作废)` / `r3 拍板 (采用)` → `R1 拍板 (作废)` / `R3 拍板 (采用)`
- line 1253 `r1 库对配 + r2 一键配 + r3 走当前配置流程` → `R1 库对配 + R2 一键配 + R3 走当前配置流程`

#### 2.5 Footer (line 1232+ 批量)

- Footer 标题: `Archery v0.5.0-r3 · DDL 跨库同步功能图说（R 拍板时 修订） · 设计人 mavis` → `Archery v0.5.0-r4 · DDL 跨库同步功能图说` (删"（R 拍板时 修订） · 设计人 mavis")
- Footer 实战数据: `DBA R 拍板时 110 prod` → 删时间
- Footer 配套: 4 个链接 R 修订/R 之前 → 2026-08-28/2026-08-21 (文件名, 不可避免)
- Footer 配套: `MD r1+r2+r3 详设` → `MD R1+R2+R3+R4 详设` + 加 R4 changelog 链接

### 3. 清理脚本 (不 commit)

**文件**: `scripts/_archive/_r5_clean_time.py` (14KB, 311 条 replace 规则)

跑完输出:
```
2026-08-28_ddl-sync-pair-design-v050-r1.md: 替换 100 处 (共 311 条规则) · 45388 bytes
2026-08-28_ddl-sync-pair-feature-card.html: 替换 27 处 (共 311 条规则) · 64377 bytes
```

后续 13+ 处 MD + 2 处 HTML 用 edit 工具精准补刀。

## 跟 r1/r2/r3/r4 关系 (r5 精修叠加)

| 版本 | 改动 | 跟 r5 关系 |
|------|------|------------|
| 8/21 v0.5.0 初版 | 库对管理 + 库对详情 + 工单联动 | R 之前 |
| 8/28 v0.5.0-r1 | 批量导入 + blacklist 默认 + 增量同步 | R1 修订 |
| 8/28 v0.5.0-r2 | 一键配机制 (1589/1289/6 min) | R2 实战数据 |
| 8/28 v0.5.0-r3 | 走当前配置的流程 | R3 重大决策 |
| 8/28 v0.5.0-r4 | 设计稿头部加"## 0. 背景" 章节 (4 部分) | R4 背景重写 |
| **8/31 v0.5.0-r5 (本次)** | **加 2 个流程图 + 全文去时间 + 删作者 + 删汇报口径** | **R5 精修** |

## 教训 (跨项目可复用, 2 条新增, 合并到 r1 entry 避免 memory 膨胀)

1. **二次开发设计稿加 2 流程图 (R 之前 + R 之后) + 优缺点在图上方** — 跟领导汇报时, 流程图比表格更直观, 优缺点放图上方让领导一眼看"现在 vs 优化后" 的优劣对比. 8/28 r4 背景重写时只用了表格, 8/31 r5 用户拍板"流程图分别展示, 优缺点在图上方" 才补 2 个 ASCII 流程图. **下次写二次开发设计稿"对比" 场景: 必加 2 个流程图 (R 之前 / R 之后), 优缺点放图上方, 不用纯表格**.
2. **设计稿精修要分 3 步走: 脚本批量 (粗) + edit 精准 (细) + grep 验证 (避免漏)** — 8/31 r5 全文时间清理用 1 个 14KB Python 脚本 (311 条 replace 规则) 一次性替换 MD 100 处 + HTML 27 处, 然后用 edit 工具补 13+ 处脚本没覆盖的 (如 r1/r2/r3 → R1/R2/R3, R 修订 在文件路径 → 2026-08-28), 最后 grep 验证 0 残留. **下次做大文档"全文 X 替换" 任务: 必演 3 步套路, 不要纯 edit 工具 (慢) 或纯脚本 (不细)**.

## 同源 entry

- 8/28 18:20 r4 背景重写 (commit `05c57cb`) - r5 改动是 r4 后的精修, 不动 r4 内容
- 8/28 16:15 r3 设计稿 (commit `e515cba`) - 改 r1+r2+r3 标识为 R1+R2+R3
- 8/28 14:00 r2 一键配 (commit `26937cb`) - 改 1589/1289/6 min 实战数据
- 8/28 09:17 r1 修订 (commit `34e2613`) - 批量导入 + blacklist 默认 + 增量同步
- 8/31 14:45 用户拍板 3 处精修 (本次 r5 触发)

## 下次推 prod 必做 (新增 2 条, 合并到 r1 entry)

1. **写二次开发设计稿"对比" 场景: 必加 2 个流程图 (R 之前 / R 之后), 优缺点放图上方** — 跟领导汇报时直观, 不用纯表格.
2. **做大文档"全文 X 替换" 任务: 必演 3 步套路 (脚本批量 + edit 精准 + grep 验证)**, 避免纯 edit 工具 (慢) 或纯脚本 (不细).
